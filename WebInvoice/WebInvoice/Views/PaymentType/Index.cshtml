@using WebInvoice.Dto.PaymentType
@model ICollection<PaymentTypeDto>
@inject WebInvoice.Services.IUserCompanyTemp userCompanyData

<div class="row">
    <partial name="~/Views/CompanySettings/_NavBarSettingsPartial.cshtml" />
    <div class="container col-md-9">
        <h3 class="text-center">@userCompanyData.CompanyName</h3>
        <h3 class="text-center">Начини на плащане</h3>
        <div class="row justify-content-center">
            <table class="table table-hover col-10">
                <thead>
                    <tr>
                        <th scope="col">Име</th>
                        <th scope="col">Описание</th>
                        <th scope="col">По подразбиране</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var paymentType in Model)
                    {
                    <tr>
                        <td>@paymentType.Name</td>
                        <td>@paymentType.Description</td>
                        <td>
                            @if (paymentType.IsActiv)
                            {
                                @:ДА
                            }
                            else
                            {
                                @:НЕ
                            }
                        </td>
                        <td><a asp-area="" asp-controller="PaymentType" asp-action="Edit" asp-route-id="@paymentType.Id" class="btn btn-dark">Редактирай</a></td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
        <div>
            <a asp-area="" asp-controller="PaymentType" asp-action="Create" class="btn btn-light">Нов начин за плащане</a>
        </div>
    </div>
</div>